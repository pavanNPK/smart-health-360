# Server
APP_PORT=3000
APP_HOST=https://api.trupathservices.com
# Frontend URL (used in OTP email link for "Set your password")
FRONTEND_URL=http://localhost:4200
# Emails use "Smart Health 360" text in the header (same as app side nav); no logo image.

# MongoDB (database: medical_coding - users collection)
DB_URL=mongodb+srv://USER:PASSWORD@cluster0.zaqfgzk.mongodb.net/medical_coding?retryWrites=true&w=majority&readPreference=primary

# JWT
JWT_SECRET=your_jwt_secret
REFRESH_TOKEN_SECRET=your_refresh_secret
JWT_EXPIRES=24h
REFRESH_TOKEN_EXPIRE=7d

# Mail (Nodemailer) - Gmail
# For Gmail: do NOT use your normal password. Use an App Password:
# 1. Enable 2-Step Verification: https://myaccount.google.com/security
# 2. Create App Password: https://myaccount.google.com/apppasswords
# 3. Put the 16-character password here (no spaces).
MAIL_HOST=smtp.gmail.com
MAIL_PORT=465
MAIL_USER=your_email@gmail.com
MAIL_PASS=your_16_char_app_password

# WhatsApp (Twilio) - optional; when set, appointment/onboarding messages are sent to patient contact phone
# Get credentials: https://www.twilio.com/console . Enable WhatsApp Sandbox: https://www.twilio.com/docs/whatsapp/sandbox
TWILIO_ACCOUNT_SID=your_account_sid
TWILIO_AUTH_TOKEN=your_auth_token
TWILIO_WHATSAPP_FROM=whatsapp:+14155238886
